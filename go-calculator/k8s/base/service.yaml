apiVersion: v1
kind: Service
metadata:
  name: go-calculator
  labels:
    app: go-calculator

spec:
  type: ClusterIP

  selector:
    app: go-calculator

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080